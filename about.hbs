THIRD-PARTY NOTICES
================================================================================
Generated by `cargo about generate about.hbs` for the shipped `turbo` binary
(`xai-grok-pager-bin`, profile `release-dist`). Do not hand-edit the crate
list below; regenerate before tagging.

PART 0 — ADAPTED SOURCE PORTS
See the committed header in git history / the previous THIRD-PARTY-NOTICES
Part 0 for oh-my-pi and other adapted ports. That narrative is preserved
in `third_party/README.md`.

================================================================================
PART I — RUST CRATES LINKED INTO THE SHIP BINARY
================================================================================

{{#each licenses}}
--------------------------------------------------------------------------------
{{name}}
--------------------------------------------------------------------------------
Used by:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}
{{/each}}

{{{text}}}
{{/each}}
