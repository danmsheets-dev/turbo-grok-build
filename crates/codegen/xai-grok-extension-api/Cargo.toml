[package]
license = "Apache-2.0"
name = "xai-grok-extension-api"
version = "0.1.0"
edition.workspace = true
description = "Shared types, capabilities, and WIT contract for Turbo WASM extensions (Pi-style lifecycle bus)"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
